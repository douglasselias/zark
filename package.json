{
  "name": "zark-lang-prototype",
  "version": "0.0.0",
  "description": "Zark: a Lisp dialect implemented in Typescript",
  "scripts": {
    "start": "ts-node --project config index.ts",
    "test": "jest --watch --config config/jest.config.ts compiler/reader.test.ts"
  },
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
